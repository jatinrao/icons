name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  # apps/admin and apps/gallery read icons at build time (ISR/static params),
  # so `next build` needs a real (if empty) database to hit — a fresh local
  # libSQL file, migrated + seeded from devicon, keeps this hermetic.
  TURSO_DATABASE_URL: file:${{ github.workspace }}/ci.db

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - run: pnpm lint
      - run: pnpm typecheck
      # All DB-touching unit tests use an in-memory libSQL instance, so this
      # doesn't need the seeded ci.db.
      - run: pnpm test

      - name: Set up a local test database (right before build, which reads it)
        run: |
          pnpm --filter @web-portfolio/icons-db db:migrate
          pnpm --filter @web-portfolio/icons-db seed-devicon
          pnpm generate-registry

      # apps/admin and apps/gallery both open the same local libSQL file
      # concurrently during `next build`'s page-data collection; serialize
      # to rule out a race in the native libsql binding under concurrent
      # access (this reproduced reliably on the Linux runner, never
      # locally on macOS with more available cores / different scheduling).
      - run: pnpm exec turbo run build --concurrency=1
