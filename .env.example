# Turso (hosted libSQL). Leave unset locally to fall back to a local
# file:./local.db — see packages/db/src/client.ts.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# apps/admin credential login
ADMIN_USER=
ADMIN_PASSWORD_HASH=
SESSION_SECRET=

# publish.yml only
NPM_TOKEN=
